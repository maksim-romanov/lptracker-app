# Subgraphs

The developer guide for this folder is [README.md](./README.md). Read it before adding or
changing a subgraph: the rule against hand-copying contract data, the `cast` workflow for
pulling verified ABIs, why ABIs are committed in `abis/`, and the checklist for a new protocol.

Per-package specifics live in each package's own `CLAUDE.md` (`uniswap-v3/`, `uniswap-v4/`).
