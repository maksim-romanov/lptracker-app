// Three-hue palette. Tweak a constant here to re-tone the whole scene — no
// look shader hard-codes a vec3.
#define PALETTE_COOL  vec3(0.72, 0.82, 1.05)
#define PALETTE_WARM  vec3(1.20, 0.68, 0.42)
#define PALETTE_WHITE vec3(1.05, 1.02, 0.98)
