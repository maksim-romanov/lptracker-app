{
  "name": "landing",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "eleventy --serve",
    "build": "bun scripts/sample-silhouette.ts && bun scripts/build-token-atlas.ts && bun scripts/build-hero.ts --mode=production && eleventy",
    "clean": "rm -rf _site src/assets/js/hero.bundle.js src/assets/data src/assets/img/token-atlas.webp",
    "lint": "biome check .",
    "lint:fix": "biome check --write .",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@11ty/eleventy": "^3.0.0",
    "lenis": "^1.1.20",
    "ogl": "^1.0.11"
  },
  "devDependencies": {
    "@depthly/theme": "workspace:*",
    "@depthly/typescript-config": "workspace:*",
    "@tweakpane/core": "^2.0.5",
    "esbuild": "^0.24.2",
    "sharp": "^0.33.5",
    "tweakpane": "^4.0.5"
  }
}
